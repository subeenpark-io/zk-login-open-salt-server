server:
  port: 3000
  host: 0.0.0.0

logging:
  level: debug
  format: pretty

security:
  corsOrigins: "*"
  rateLimitMax: 100
  rateLimitWindowMs: 60000

provider:
  type: remote
  endpoint: "https://salt.api.mystenlabs.com/get_salt"
  timeout: 10000
  retryCount: 2
